{
  "entities": {
    "Order": {
      "title": "Order",
      "description": "A customer order in the Krispy King system",
      "type": "object",
      "properties": {
        "id": { "type": "string", "maxLength": 128 },
        "passCode": { "type": "string", "pattern": "^KK-[A-Z0-9]+$" },
        "customerName": { "type": "string", "maxLength": 100 },
        "items": { "type": "array", "description": "List of cart items containing menuItem, quantity, unitPrice and selectedOptions" },
        "total": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "verified", "completed", "cancelled"] },
        "createdAt": { "type": "number" },
        "verifiedAt": { "type": "number" },
        "completedAt": { "type": "number" }
      },
      "required": ["id", "passCode", "customerName", "items", "total", "status", "createdAt"]
    },
    "MenuStatus": {
      "title": "MenuStatus",
      "description": "Menu item availability status map where keys are item IDs and values are booleans",
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "firestore": {
    "orders/{orderId}": {
      "schema": "#/entities/Order",
      "description": "Order records for customers showing queue number, status, total price, and timestamps"
    },
    "admin_config/menu_status": {
      "schema": "#/entities/MenuStatus",
      "description": "Document tracking which menu items are currently available"
    }
  }
}
